# ============================================================================
# env/gemini.env — Google Gemini (ONE key for everything Gemini)
# Copy to env/gemini.env and fill in. Powers: the agent planner/synthesis, RAG
# embeddings (gemini-embedding-2), and the eval judge — all use this single key.
# Get one at https://aistudio.google.com/apikey
# ============================================================================

GOOGLE_API_KEY=
